/*
 * This file intentionally stays in place after the CLI tests were split into
 * focused files.
 *
 * Keeping it prevents stale build/main/main.test.js output from older builds
 * from being executed by node:test after TypeScript recompilation.
 */
export {};
