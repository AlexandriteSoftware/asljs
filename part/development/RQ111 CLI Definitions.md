# RQ111 CLI Definitions

CLI tool searches for definitions by enumerating markdown files and
checking whethe the content of the file matches [DF1 Definition][1].

When CLI has parameter `Definitions` with specified path to the folder, the CLI
looks for the definitions in the specified path only.

[1]: <DF1 Definition.md>
