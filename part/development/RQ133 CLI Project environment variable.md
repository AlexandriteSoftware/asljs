# RQ133 CLI Project environment variable

When environment variable `PART_PROJECT` is set, the CLI uses the path
specified in the environment variable as the project root.

CLI parameter `--project` has higher priority than the environment variable.
