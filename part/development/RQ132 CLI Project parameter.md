# RQ132 CLI Project parameter

The CLI tools resolves absolutre paths based on the project root.
By default, the project root is the current working directory. However, if
the CLI has parameter `--project` with specified path to the folder, the CLI
considers that folder as the project root.
