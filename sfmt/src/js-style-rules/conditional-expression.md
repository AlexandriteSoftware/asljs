# conditional-expression

## Tests

```js
const result = condition ? whenTrue : whenFalse;
// ---
const result = condition
  ? whenTrue
  : whenFalse;
```

```js
const result =
  condition
  ? whenTrue
  : whenFalse;
// ---
const result =
  condition
  ? whenTrue
  : whenFalse;
```