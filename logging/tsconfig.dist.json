{
  "extends": "../tsconfig.dist.json",
  "include": ["src/**/*.ts"],
  "exclude": ["build", "dist", "test", "**/*.test.ts"],
  "compilerOptions": {
    "types": ["node"],
    "rootDir": "src",
    "outDir": "dist"
  }
}
