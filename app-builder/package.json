{
  "name": "asljs-app-builder",
  "version": "0.1.0",
  "description": "Browser-based AI-assisted local-first app builder — GitHub Pages front page and demo for ASLJS.",
  "private": true,
  "type": "module",
  "scripts": {
    "format:json": "dprint fmt --config ../dprint.json \"*.json\"",
    "format:ts": "dprint fmt --config ../dprint.json \"src/**/*.ts\"",
    "format:sfmt": "sfmt \"src/**/*.ts\"",
    "format": "npm run format:json && npm run format:ts && npm run format:sfmt",
    "clean": "node ../common/dist/toolkit.js clean",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "preview": "vite preview",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "all": "npm run clean && npm run format",
    "all:todo": "npm run typecheck && npm run lint && npm run build:dist && npm run build && npm run test"
  },
  "dependencies": {
    "asljs-components": "^0.1.3",
    "asljs-dali": "^0.1.4",
    "asljs-data-binding": "^0.2.4",
    "asljs-eventful": "^0.4.10",
    "asljs-observable": "^0.5.4",
    "esbuild": "^0.28.1",
    "esbuild-wasm": "^0.28.1"
  }
}
